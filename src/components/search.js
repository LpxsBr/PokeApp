export default{
    
    data() {
        return {
            name: 'Vue.js'
        }
    },
    methods: {
        greet(event) {
            if (event) {
                console.log(event.target.value)
                }
            return value = event.target.value
            }
        }
    }